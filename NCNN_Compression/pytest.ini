[pytest]
testpaths = tests
addopts = -ra
markers =
    unit: isolated tests for one module or function
    integration: tests that combine several project components with external tools mocked
    smoke: fast end-to-end checks of the main pipeline using fake backends
    external: tests that require real Android/NCNN/Ultralytics/ONNX Runtime resources
