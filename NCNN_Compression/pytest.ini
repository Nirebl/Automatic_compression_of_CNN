[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast unit tests with mocked external dependencies
    integration: integration-level tests for combined pipeline components
    smoke: end-to-end smoke tests for the fake pipeline
